// Test both comma operator and comma-separated declarations
let result = (1, 2, 3);
console.log("Comma operator result:", result);

// Test simple comma-separated declarations (only first variable is stored due to simplified implementation)
let a = 10;
console.log("Variable a:", a);