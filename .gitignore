# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with 'go test -c'
*.test

# Output of the go coverage tool
*.out

# Dependency directories
vendor/

# Go workspace file
go.work

# IDE specific files
.idea/
.vscode/
*.swp
*.swo 
pkg/.DS_Store
/paserati
paserati-minimal

*.baseline
*.pprof
gojac
qjs
.DS_Store

# Python virtual environment
venv/
__pycache__/
*.pyc

timing.py
timing.json
.tally.py
scratch/
# Test262 test suite (cloned by setup-test262.sh) — anchored so it
# doesn't also swallow pkg/test262/ (the shared result-types package).
/test262/
/paserati-test262
*.prof
baseline.txt
mcp-go-debugger.log
dome.js
# Built binaries — anchored so they don't also swallow cmd/paserati-*/ source dirs.
/paserati-analyze
/paserati-v8bench
/bench-ratchet
/bench-test262
bench/out/
quickjs-bench/
benchmarks/
# bench-ratchet raw captures (the committed artifact is docs/perf/baseline.json only)
docs/perf/.runs/
stvm*.js
paserati-v8bench-prof
/paserati-testtsc
typescript.js
GEMINI.md
CLAUDE.md
.claude/
.cursor/
