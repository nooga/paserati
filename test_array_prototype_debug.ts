// Simple test to check if Array.prototype.slice exists
let arr = [1, 2, 3];
console.log("Array:", arr);
console.log("Array length:", arr.length);
console.log("Array push exists:", typeof arr.push);
console.log("Array slice exists:", typeof arr.slice);