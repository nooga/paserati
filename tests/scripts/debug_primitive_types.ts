// Debug which primitive types are available
let a: any;         // Should work
let n: number;      // Check if number works
let s: string;      // Check if string works
let b: boolean;     // Check if boolean works

console.log("Type check");
"Type check"; // Final expression

// expect: Type check